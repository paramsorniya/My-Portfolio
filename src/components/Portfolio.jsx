import { useRef } from "react";
import "./portfolio.scss";
import { motion, useScroll, useSpring, useTransform } from "framer-motion";

const items = [
  {
    id: 1,
    title: "Supply Chain Management",
    img: "/Supplychain.png",
    desc: "This project explores the use of blockchain technology to revolutionize traditional supply chain management. By leveraging a secure and transparent distributed ledger",
  },
  {
    id: 2,
    title: "Text Utils",
    img: "/textutils.png",
    desc: "This project is a text formatter tool that helps you to enhance the appearance and readability of your plain text. You can Uppercase, Lowercase & Remove Extra Spaces and many more.",
  },
  {
    id: 3,
    title: "Portfolio Website",
    img: "/portfolio.png",
    desc: "This is my Personal Portfolio website, where you can get all the information about my skills and projects.",
  },
  
];

const Single = ({ item }) => {
  const ref = useRef();

  const { scrollYProgress } = useScroll({
    target: ref,
  });

  const y = useTransform(scrollYProgress, [0, 1], [-300, 300]);

  return (
    <section >
      <div className="container">
        <div className="wrapper">
          <div className="imageContainer" ref={ref}>
            <img src={item.img} alt="" />
          </div>
          <motion.div className="textContainer" style={{y}}>
            <h2>{item.title}</h2>
            <p>{item.desc}</p>
            <button>See Demo</button>
          </motion.div>
        </div>
      </div>
    </section>
  );
};

const Portfolio = () => {
  const ref = useRef();

  const { scrollYProgress } = useScroll({
    target: ref,
    offset: ["end end", "start start"],
  });

  const scaleX = useSpring(scrollYProgress, {
    stiffness: 100,
    damping: 30,
  });

  return (
    <div className="portfolio" ref={ref}>
      <div className="progress">
        <h1>Featured Works</h1>
        <motion.div style={{ scaleX }} className="progressBar"></motion.div>
      </div>
      {items.map((item) => (
        <Single item={item} key={item.id} />
      ))}
    </div>
  );
};

export default Portfolio;